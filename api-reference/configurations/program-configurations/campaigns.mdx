---
title: "Campaigns Configurations"
description: "Retrieve active campaigns configuration for the loyalty program."
openapi: 'GET /api/v4.0/integrations/configurations/program/campaigns'
languages: ["curl", "javascript", "python", "csharp"]
---

## Campaigns Configurations

Retrieve active campaigns configuration for the loyalty program.

<Info>
**Note:** This endpoint needs the specific content from the old documentation. Please provide the request/response schemas and examples so I can create the complete OpenAPI-driven documentation.
</Info>

## Response

<ResponseField name="campaigns" type="array">
Array of active campaigns configuration.

<ResponseField name="campaigns[].id" type="string">
Unique identifier for the campaign.
</ResponseField>

<ResponseField name="campaigns[].name" type="string">
Name of the campaign.
</ResponseField>

<ResponseField name="campaigns[].description" type="string">
Description of the campaign.
</ResponseField>

<ResponseField name="campaigns[].startDate" type="string">
Campaign start date.
</ResponseField>

<ResponseField name="campaigns[].endDate" type="string">
Campaign end date.
</ResponseField>

<ResponseField name="campaigns[].isActive" type="boolean">
Whether the campaign is currently active.
</ResponseField>

<ResponseField name="campaigns[].rewardType" type="string">
Type of reward offered by the campaign.
</ResponseField>
</ResponseField>
