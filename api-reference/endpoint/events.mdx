---
title: 'Events'
openapi: 'POST /api/v4.0/integrations/events'
---

## Send Events

Track customer actions to power rewards and engagement.

<Endpoint
  method="POST"
  host="https://api.gameball.co"
  path="/api/v4.0/integrations/events"
  authenticated
>
  <Request>
    <Headers>
      <Header name="apikey" type="string" required />
      <Header name="secretkey" type="string" required />
      <Header name="Content-Type" example="application/json" />
    </Headers>
    <Body type="application/json">
      {{
        "customerId": "1848877205",
        "events": {
          "write_review": {
            "product_id": "1653503260",
            "review": "5 Stars Product"
          }
        }
      }}
    </Body>
  </Request>

  <TryIt />

  <CodeGroup>
    <Code name="curl">
curl -X POST \
  https://api.gameball.co/api/v4.0/integrations/events \
  -H 'Content-Type: application/json' \
  -H 'apikey: <YOUR_API_KEY>' \
  -H 'secretkey: <YOUR_SECRET_KEY>' \
  -d '{
    "customerId": "1848877205",
    "events": {
      "write_review": {
        "product_id": "1653503260",
        "review": "5 Stars Product"
      }
    }
  }'
    </Code>
    <Code name="javascript">
await fetch('https://api.gameball.co/api/v4.0/integrations/events', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'apikey': process.env.GAMEBALL_API_KEY,
    'secretkey': process.env.GAMEBALL_SECRET_KEY
  },
  body: JSON.stringify({
    customerId: '1848877205',
    events: {
      write_review: { product_id: '1653503260', review: '5 Stars Product' }
    }
  })
})
    </Code>
    <Code name="python">
import os, requests

url = 'https://api.gameball.co/api/v4.0/integrations/events'
headers = {
  'Content-Type': 'application/json',
  'apikey': os.getenv('GAMEBALL_API_KEY'),
  'secretkey': os.getenv('GAMEBALL_SECRET_KEY')
}
payload = {
  'customerId': '1848877205',
  'events': {
    'write_review': {
      'product_id': '1653503260',
      'review': '5 Stars Product'
    }
  }
}
res = requests.post(url, json=payload, headers=headers)
print(res.status_code, res.json())
    </Code>
  </CodeGroup>

  <Responses>
    <Response status={200}>
      <Body type="application/json">
        {{ "success": true, "message": "Events processed" }}
      </Body>
    </Response>
    <Response status={400} example>
      <Body type="application/json">
        {{ "error": 400, "message": "Invalid event payload" }}
      </Body>
    </Response>
    <Response status={401}>
      <Body type="application/json">
        {{ "error": 401, "message": "Unauthorized" }}
      </Body>
    </Response>
    <Response status={429}>
      <Body type="application/json">
        {{ "error": 429, "message": "Too Many Requests" }}
      </Body>
    </Response>
  </Responses>
</Endpoint>

<Callout>
Security: Requires <code>apikey</code> and <code>secretkey</code> headers.
</Callout>

<Callout>
Reference: See Gameball Events docs for concepts and examples.
</Callout>


